 <hr>
<div class="container py-5">
    <h2 class="text-center mb-4 text-primary">📢 Offres d'emploi récentes</h2>
    <p class="text-center text-muted">Retrouvez des centaines d'offres d'emploi dans différents secteurs et villes.</p>
    <?php if (count($offres) > 0): ?>
        <div class="row">
            <?php foreach ($offres as $offre): ?>
                <div class="col-lg-4 col-md-6 mb-4">
                    <div class="card shadow-sm border-0 rounded-3">
                        <div class="card-body">
                            <div class="d-flex align-items-center">
                                <div class="me-3">
                                    <img src="<?= htmlspecialchars($offre['logo'] ?? 'default.png') ?>" alt="Logo" width="40" height="40" class="rounded-circle">
                                </div>
                                <div>
                                    <h5 class="card-title mb-0">
                                        <a href="offre_emploi.php?id=<?= $offre['id_offre'] ?>" class="text-decoration-none text-dark">
                                            <?= htmlspecialchars($offre['titre']) ?>
                                        </a>
                                    </h5>
                                    <h6 class="card-subtitle text-muted">
                                        <?= htmlspecialchars($offre['nom_entreprise'] ?? 'Recruteur inconnu') ?>
                                    </h6>
                                </div>
                            </div>

                           <h5 class="card-title"><?= htmlspecialchars($offre['titre'] ?? '') ?></h5>
                            <h6 class="card-subtitle text-muted">
                             <?= htmlspecialchars($offre['nom_entreprise'] ?? 'Recruteur inconnu') ?>
                            </h6>
                           <ul>
                             <li><i class="bi bi-geo-alt-fill"></i> <?= htmlspecialchars($offre['lieu'] ?? 'Lieu non spécifié') ?></li>
                             <li><i class="bi bi-clock"></i> Expire le : <?= date('d/m/Y', strtotime($offre['date_expiration'] ?? 'now')) ?></li>
                           </ul>
                            <div class="d-flex justify-content-between align-items-center mt-3">
                                <a href="offre_emploi.php?id=<?= $offre['id_offre'] ?>" class="btn btn-outline-primary btn-sm">Voir l’offre</a>
                                <button class="btn btn-sm btn-light">
                                    <i class="bi bi-bookmark"></i> Sauvegarder
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            <?php endforeach; ?>
        </div>

        <div class="text-center mt-4">
            <a href="offres.php" class="btn btn-primary">📜 Voir toutes les offres</a>
        </div>

    <?php else: ?>
        <div class="alert alert-info text-center">Aucune offre disponible pour le moment.</div>
    <?php endif; ?>
</div>
